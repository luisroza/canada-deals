# Frontend testing

Validated toolchain (2026-08-20): Node.js 24 and the repository-declared `pnpm@10.15.0`.

Run component tests and a production build:

```powershell
pnpm --dir apps/web test
pnpm --dir apps/web build
```

The 86-test component/library suite covers the current store-led, Wishlist-only product plus all retained trust, report, account, search, and safe affiliate behaviors. API boundary coverage proves the Production server still requires `API_BASE_URL`, the browser resolves API calls through same-origin routes, and an explicitly configured server origin is preserved. Product-route recovery coverage verifies that an unknown slug presents clear search, current-deals, and Wishlist actions without visible generic `404` copy. Deal Card coverage verifies the concise hierarchy, supported savings, single freshness/evidence line, retailer action, unavailable-link state, Product imagery, title navigation, and signed-out Wishlist return context. Wishlist coverage includes all card-level accessible-name states (`Save`, `Saved`, `Wait`, loading, unavailable, and signed-out), one shared session/list load, save/remove state, synchronized navigation count, local search/sort controls, exclusive error state, and retry. Store banner coverage includes selective first-image eager/high priority with remaining images lazy, visible-copy accessible names, separate new-tab descriptions, ACTIVE protected new-tab handoff, DISCOVERY_ONLY internal navigation, missing-asset fallback, disabled state, raw-URL and incomplete-activation fail-closed behavior, commercial-state labeling, all enabled profiles in supplied neutral order, responsive one/two/four-banner pagination, a discrete one-banner mobile slide with button and swipe navigation, accessible carousel controls, owner selection, public-state distinctions, reviewed artwork selection/upload affordance, provenance controls, and one source of truth for selection. Catalog-menu coverage verifies consistent marker/label structure for short and long store names and mutually exclusive Category/Store expansion. Owner-panel coverage includes independent Brand/Category/Store navigation, immutable identifiers, Product reuse, optional offer validity, progressive disclosure, Canadian market presentation, and absence of destructive delete controls. CSP and development-origin coverage prove local private-network HTTP assets remain loadable only for explicitly allowed LAN hosts while Production retains insecure-request upgrading.

A clean Chromium 151 production Lighthouse run with a temporary profile and extensions disabled scored Desktop `100/100/96/100` and Mobile `98/100/96/100` for Performance/Accessibility/Best Practices/SEO. Label-in-name mismatches, application hydration errors, and horizontal mobile overflow were absent; CLS was `0.0015` Desktop and `0` Mobile. The first store-banner image loaded eager/high while later images stayed lazy, and Mobile downloaded only the active first image. The remaining low-risk first-party finding is `/favicon.ico` returning 404, which keeps Best Practices at 96 and is follow-up work outside this focused correction.

The 26-test Playwright suite runs against real Next.js, ASP.NET Core, PostgreSQL, and the separately hosted Hangfire worker with no core API interception. Store banner journeys verify the controlled backend 302 and click record without navigating to an external provider, plus internal store-filtered discovery. Product recovery verifies the friendly unavailable state while preserving the HTTP 404 response. The complete suite has zero skips. Install Chromium once with `pnpm --dir apps/web exec playwright install chromium`.
