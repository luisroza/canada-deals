# Backend Documentation

Start with `BACKEND.md` for the application boundary, `API.md` for HTTP contracts, `DATABASE.md` for persistence, `JOBS.md` for worker behavior, and `EMAIL.md` for account-confirmation, Resend, retry/idempotency, webhook, suppression, and operational activation.
